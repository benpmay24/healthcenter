{
  "name": "healthcenter",
  "private": true,
  "description": "Health tracking app — root for local dev; deploy API and frontend as two separate services",
  "scripts": {
    "dev:api": "npm run dev --prefix api",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
