{
  "name": "healthcenter",
  "private": true,
  "description": "Health tracking app — monorepo root for Render",
  "scripts": {
    "build": "cd api && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd api && npm start"
  }
}
