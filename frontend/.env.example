# Copy to .env and set at build time if your API is on a different origin.
# See README "Production deployment" section.

# API base URL (no trailing slash). Omit when using same-origin /api or a reverse proxy.
# VITE_API_URL=https://api.myapp.com
