{
  "name": "healthcenter-api",
  "version": "1.0.0",
  "description": "Health tracking API",
  "main": "server.js",
  "type": "module",
  "engines": { "node": ">=20 <25" },
  "scripts": {
    "start": "node server.js",
    "start:with-install": "corepack enable && yarn install --frozen-lockfile && node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
