# Copy to .env and fill in. See README for production deployment.

# PostgreSQL (production). Omit to use SQLite (dev).
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Port the API listens on (default 3001)
# PORT=3001

# USDA FoodData Central API key (recommended for food search)
# FDC_API_KEY=your_key_here

# Allowed CORS origin (production). Omit to allow all origins.
# CORS_ORIGIN=https://myapp.com
